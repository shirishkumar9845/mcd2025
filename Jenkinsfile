pipeline {

  agent any
  stages {

    stage('git checkout') {
        steps {
            script {
              printIn "Hello all welcome to pipelinescripting"
            }
        }
    }
  }
}
