pipeline {
    agent any
    stages {
        stage('git checkout') {
            steps {
                printIn "Hello all welcome to pipelinescripting"
                sh 'echo "This is a shell command output."'
            }
        }
    }
  }
