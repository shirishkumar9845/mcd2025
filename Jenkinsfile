pipeline {

  agent slave1
  stages {

    stage('git checkout') {
        steps {
            script {
              printIn "Hello all welcome to pipelinescripting"
            }
        }
    }
  }
}
