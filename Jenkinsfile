pipeline {
    agent any
    stages {
        stage('git checkout') {
            steps {
              script {
               echo  'Hello all welcome to pipelinescripting'
            }
        }
    }
  }
}
